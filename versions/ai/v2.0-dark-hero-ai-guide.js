// ============================================================
// 📋 주인공 AI 시스템 통합 가이드 (Claude Code용)
// ============================================================
// 이 파일은 설명서입니다. 코드에 넣지 마세요.
// 아래 내용을 읽고 기존 게임 코드에 통합해주세요.
// ============================================================

/*
═══════════════════════════════════════════════════════════════
🎯 목표: 주인공(영웅)에게 AI 시스템을 탑재하는 것
═══════════════════════════════════════════════════════════════

■ 현재 문제:
  - 영웅, 정령, 펫이 각각 따로 놀고 있음
  - 장비를 바꿔도 외모가 안 바뀜
  - 정령이 소모품인데 시스템이 없음
  - 전투에서 AI가 스킬을 자동 선택하는 로직이 없음

■ 해결: 3개 파일을 게임에 통합
  1. hero-ai-core.js    → 파티 구성 + 스탯 계산
  2. hero-ai-battle.js  → 전투 중 AI 의사결정
  3. hero-ai-visual.js  → 스킬 연출 + 외모 반영

═══════════════════════════════════════════════════════════════
📂 파일 위치
═══════════════════════════════════════════════════════════════

C:\Users\PC\Downloads\ 에서 아래 3개 파일을 복사:
  → C:\Users\PC\Documents\monglebel\js\ai\hero-ai-core.js
  → C:\Users\PC\Documents\monglebel\js\ai\hero-ai-battle.js
  → C:\Users\PC\Documents\monglebel\js\ai\hero-ai-visual.js

index.html에 스크립트 추가 (순서 중요!):
  <script src="js/ai/hero-ai-core.js"></script>
  <script src="js/ai/hero-ai-battle.js"></script>
  <script src="js/ai/hero-ai-visual.js"></script>

═══════════════════════════════════════════════════════════════
🔗 기존 코드에 연결하는 방법
═══════════════════════════════════════════════════════════════

【1】 게임 초기화 시 (game.js 또는 main.js)
──────────────────────────────────────────
기존 영웅 생성 코드를 찾아서, HeroAI.createHero()로 감싸줘.

변경 전 (예시):
  const hero = { name: '전사', class: 'warrior', level: 5, ... };

변경 후:
  const hero = HeroAI.createHero({
    name: '전사',
    class: 'warrior',
    element: 'fire',
    rarity: 'rare',
    level: 5,
    skinTone: 'light',
    hairStyle: 'spiky',
    weapon: playerInventory.equippedWeapon,   // 기존 장비 데이터 연결
    armor: playerInventory.equippedArmor,
    skills: ['slash', 'heavy_strike', 'whirlwind', 'war_cry'],
    aiPersonality: 'balanced'
  });
  HeroAI.addHero(hero);


【2】 소환의 나무에서 정령 소환할 때
──────────────────────────────────────────
소환 결과를 HeroAI에 등록해줘.

  // 소환의 나무 소환 결과
  const spirit = HeroAI.createSpirit({
    name: '화염 정령',
    element: 'fire',
    rarity: 'epic',
    level: 10,
    skill: 'fire_spirit_strike',  // 정령 고유 스킬
    uses: 1  // 소모품 (1회용)
  });
  HeroAI.addSpirit(spirit);

  // 소환 연출
  HeroAIVisual.playSpiritSummon(spirit, scene);


【3】 펫 장착할 때
──────────────────────────────────────────
  const pet = HeroAI.createPet({
    name: '몽글이',
    type: 'cat',
    element: 'light',
    rarity: 'rare',
    level: 8,
    skill: 'pet_heal_lick',
    passive: 'luck_boost',  // 패시브: 행운 +5%
    collar: equippedCollar   // 펫 장비
  });
  HeroAI.setPet(pet);


【4】 ⭐ 스테이지 입장 직전 (가장 중요!)
──────────────────────────────────────────
스테이지 시작 함수에서 calculateAll()을 호출해야 해.
이게 모든 스탯을 합산하고, 장비 외모를 반영하고, 정령 보너스를 분배해.

기존 스테이지 시작 코드를 찾아서 추가:

  function startStage(stageId) {
    // ★ 이 한 줄 추가! ★
    const partyData = HeroAI.calculateAll();
    
    // partyData에 모든 정보가 들어있음:
    // partyData.heroes    → 영웅 5명 (최종 스탯, 스킬, 외모 포함)
    // partyData.spirits   → 정령 리스트
    // partyData.pet       → 펫 정보
    // partyData.totalPower → 총 전투력
    
    // 기존 스테이지 시작 로직...
    loadStage(stageId);
    spawnEnemies(stageId);
    // ...
  }


【5】 전투 중 - 영웅 턴이 올 때마다
──────────────────────────────────────────
기존 전투 턴 로직에서, 영웅의 행동을 AI가 결정하게 해.

  function onHeroTurn(hero) {
    // AI가 행동 결정
    const action = HeroBattleAI.decideTurn(
      hero,                    // 현재 턴 영웅
      battleState.enemies,     // 적 배열
      HeroAI.party.heroes,     // 아군 배열
      { turn: currentTurn }    // 전투 상태
    );

    // 결정된 행동 실행
    switch (action.type) {
      case 'attack':
        executeAttack(hero, action.target);
        break;
      case 'skill':
        executeSkill(hero, action.skillId, action.target);
        // 스킬 연출
        HeroAIVisual.playSkillEffect(action.skillData, hero, action.target, scene);
        break;
      case 'defend':
        executeDefend(hero);
        break;
    }
  }


【6】 전투 중 - 펫 자동 행동 (매 턴 끝)
──────────────────────────────────────────
  function onTurnEnd() {
    if (HeroAI.party.pet) {
      const petAction = HeroBattleAI.petAutoAction(
        HeroAI.party.pet,
        HeroAI.party.heroes,
        battleState.enemies
      );
      if (petAction) {
        executePetAction(petAction);
      }
    }
  }


【7】 전투 중 - 데미지 계산
──────────────────────────────────────────
기존 데미지 계산 코드를 교체하거나 연동:

  // 기존: const damage = hero.atk - enemy.def;
  // 변경:
  const result = HeroBattleAI.calculateDamage(hero, enemy, skillData);
  // result.damage → 최종 데미지
  // result.isCrit → 크리티컬 여부
  // result.isEffective → 원소 상성 효과적
  // result.element → 사용 원소


【8】 장비 변경할 때 - 외모 자동 반영
──────────────────────────────────────────
영웅의 장비를 바꿀 때마다 호출:

  function equipItem(hero, slot, item) {
    hero.equipment[slot] = item;
    HeroAI._calculateHeroStats(hero);      // 스탯 재계산
    HeroAI._updateHeroAppearance(hero);    // 외모 업데이트
    
    // 3D 모델에 반영
    if (hero.model3d) {
      HeroAIVisual.updateHeroModel(hero, hero.model3d);
    }
  }


【9】 정령 사용 (전투 중 소모)
──────────────────────────────────────────
  function useSpiritInBattle(spiritId) {
    HeroAI.consumeSpirit(spiritId);  // 소모품이라 사라짐
    // 정령 스킬은 이미 영웅에게 부여된 상태
  }


═══════════════════════════════════════════════════════════════
📊 데이터 흐름 요약
═══════════════════════════════════════════════════════════════

  [소환의 나무] → 정령 소환 (소모품) → HeroAI.addSpirit()
       ↓
  [파티 편성 화면] → 영웅 5명 배치 → HeroAI.addHero()
       ↓                펫 장착    → HeroAI.setPet()
       ↓                장비 장착  → hero.equipment 업데이트
       ↓
  [스테이지 입장 버튼 클릭]
       ↓
  ★ HeroAI.calculateAll() ★  ← 이 순간 모든 것이 계산됨
       ↓
  - 영웅 기본 스탯 계산 (레벨 + 클래스)
  - 장비 스탯 합산
  - 등급 배율 적용
  - 원소 보너스 적용
  - 정령 보너스 분배 (같은 원소 영웅 우선)
  - 정령 스킬 → 영웅에게 부여
  - 펫 보너스 → 파티 전체 적용
  - 파티 시너지 계산 (원소 시너지, 역할 시너지)
  - 장비 → 외모 반영 데이터 업데이트
  - 전체 HP/MP 풀 회복
       ↓
  [전투 시작]
       ↓
  매 턴: HeroBattleAI.decideTurn() → 행동 결정
         HeroAIVisual.playSkillEffect() → 연출
         HeroBattleAI.petAutoAction() → 펫 자동 행동
       ↓
  [전투 종료] → 정령 소모품 사라짐, 결과 처리

═══════════════════════════════════════════════════════════════
⚠️ 주의사항
═══════════════════════════════════════════════════════════════

1. hero-ai-core.js가 제일 먼저 로드되어야 함
2. calculateAll()은 반드시 스테이지 입장 직전에 호출
3. 정령은 소모품! 사용하면 사라짐 (소환의 나무에서 다시 소환해야 함)
4. 영웅 최대 5명, 정령 최대 10마리, 펫 최대 1마리 제한
5. 장비 변경 시 외모 자동 반영 (equipItem 함수 사용)
6. HeroBattleAI.SKILLS에 모든 스킬 데이터가 있음
7. 기존 전투 코드의 데미지 계산을 HeroBattleAI.calculateDamage()로 교체

*/
